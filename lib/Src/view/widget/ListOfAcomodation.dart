import 'package:flutter/material.dart';

class ListOfAcommodation extends StatelessWidget {
  const ListOfAcommodation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
